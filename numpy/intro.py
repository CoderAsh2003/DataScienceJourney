import numpy as np

a = np.array([1,2,3]) 

#A numpy array is very similar to a list but has several benefits.
# 1. It takes lesser space, much faster and more convinient


#Now lets compare numpy with a list and see the difference
